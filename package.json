{
  "name": "make-deploy",
  "version": "1.0.0",
  "description": "",
  "main": "dist/make-deploy.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "node dist/make-deploy.js echo hello"
  },
  "bin": {
    "make-deploy": "dist/make-deploy.js"
  },
  "engines": {
    "node": ">=8.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:redzumi/make-deploy.git"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cross-spawn": "^7.0.2",
    "string-argv": "^0.3.1"
  },
  "devDependencies": {
    "@types/node": "^14.0.1",
    "typescript": "^3.9.2"
  }
}
